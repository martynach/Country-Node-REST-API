Hej, zerknąłem w końcu więc moje uwagi:

Najpierw konkretne do tego kodu:
* przydałaby się jakaś obsługa błędów czyli np. zwracanie 404 jak kraj nie istnieje
* błędy jak “No country codes!” powinny mieć kod błędów i też być JSONami skoro to JSONowe API.
* co do matematyki, zrobiłaś wszystkie 3 jednym kodem, miałem nadzieję, że mniej więcej tak to będzie ale zamiast przekierowania można było zrobić to co masz w /math jako funkcję i obsługiwać pod oryginalnymi adresami

I kilka drobniejszych:
* widzę mix async/await i then/catch, fajnie by to było jednolite
* pewne rzeczy można uprościć, np. zamiast 
	return new Promise(res => res({code, msg: "No data"}));
	wystarczy
	return {code, msg: "No data"}
	poza tym między innymi map w linii 15,  if w linii 130 i pewnie kilka innych
* klucz do API w repozytorium TODO

A tak poza tym:
* nie ma gitignore TODO
* nie ma lockfile (yarn.lock albo package-lock.json), trochę więcej: https://yarnpkg.com/blog/2016/11/24/lockfiles-for-all/
* możesz się zainteresować programami pomagającymi dbać o kod (prettier, eslint, airbnb) TODO
* spróbuj deployować na heroku 😀 TODO

Jak  coś to w poniedziałek wieczorem będę dostępny